// values must correspond to id names in public html file
export enum HTMLHooks {
    Drawer = 'drawer-hook',
    Backdrop = 'backdrop-hook',
    Modal = 'modal-hook'
}

export const RULE = {
    PW_MIN_LEN: 8,
    PW_MAX_LEN: 32,
    USR_MIN_LEN: 4,
    USR_MAX_LEN: 16,
    DEFAULT_MAX_LEN: 32,
    DES_MAX_LEN: 128
};
